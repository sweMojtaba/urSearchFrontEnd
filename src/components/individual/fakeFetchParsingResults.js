function fakeFetchParsingResults(role) {
    if (role === "individual") {
        const parsingResults = {
            personalInfo: {
                name: "Mojtaba Javid",
                degree: "B.S."
                
            }
        }
    }
}